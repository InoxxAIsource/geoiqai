Add password-based account creation
alongside the existing magic link system.

=== AUTH UPGRADE ===

Current state:
- Only magic link login exists
- No way to create account with password
- No way to set password after payment
- Users frustrated checking email every time

Required state:
- Magic link still works (keep it)
- Users can also create password
- After payment: option to set password
- Sign in page has both options

=== STEP 1: DATABASE ===

Add password field to users table:
ALTER TABLE users 
ADD COLUMN IF NOT EXISTS 
password_hash TEXT;

ADD COLUMN IF NOT EXISTS
email_verified BOOLEAN DEFAULT false;

=== STEP 2: SIGN UP PAGE (/signup) ===

Create new page at /signup

Layout: centered card, max-width 400px

Title: "Create your GeoIQ account"
Subtitle: "Already have an account? 
           Sign in →" (links to /login)

Form fields:
Email input
Password input (min 8 characters)
Confirm password input

Password requirements shown:
"At least 8 characters"
Show/hide toggle on password field

[Create account →] button
bg: #4F46E5, full width, height 44px

On submit:
1. Validate email format
2. Validate password min 8 chars
3. Validate passwords match
4. Check if email exists in users table
   If exists: "Account already exists.
   Sign in instead →"
5. Hash password with bcrypt
   (saltRounds: 12)
6. Insert into users table:
   email, password_hash,
   plan: 'free',
   email_verified: false
7. Send verification email via Resend:
   Subject: "Verify your GeoIQ email"
   Body: "Click to verify your account"
   Link: /auth/verify-email?token=xxx
8. Redirect to /verify-email-sent page:
   "Check your email to verify 
    your account"

=== STEP 3: SIGN IN PAGE (/login) ===

Update existing login page.

Currently shows only magic link.

New design — two options:

OPTION A: Email + Password
Email input
Password input with show/hide toggle
[Sign in →] button

Forgot password link below button:
"Forgot password? Reset it →"
Links to /forgot-password

OPTION B: Magic link  
"Or sign in with a magic link"
Email input
[Send magic link →] button
"We'll email you a one-click login link"

Divider between options:
"────── or ──────"

On password sign in:
1. Find user by email
2. Check password_hash with bcrypt.compare
3. If wrong: "Incorrect email or password"
   (never say which one is wrong)
4. If correct and email not verified:
   "Please verify your email first.
    Resend verification →"
5. If correct and verified:
   Create session/JWT token
   Redirect to /dashboard

=== STEP 4: AFTER PAYMENT — SET PASSWORD ===

After user pays and clicks magic link,
they land on dashboard.

If user has NO password set yet
(password_hash is null):

Show one-time banner at top of dashboard:
bg: #EEF2FF
border: 1px solid #C7D2FE
border-radius: 8px
padding: 12px 16px
display: flex
align-items: center
justify-content: space-between

Left text:
"Set a password for easier sign in"
font-size: 13px, color: #3730A3

Right: [Set password →] button
outlined, small, indigo

On click: show modal:

Modal title: "Set your password"
New password input
Confirm password input
[Save password] button

On save:
Hash with bcrypt
Update users table: password_hash
Mark: email_verified = true
(payment email is already verified)
Show: "Password set. 
You can now sign in with 
your email and password."
Dismiss banner permanently

=== STEP 5: FORGOT PASSWORD (/forgot-password) ===

Simple page:

Title: "Reset your password"
Email input
[Send reset link →] button

On submit:
1. Find user by email
2. If not found: still show success
   (security — don't reveal emails)
3. If found: create reset token
   (similar to magic_tokens table)
   expires in 1 hour
4. Send email:
   Subject: "Reset your GeoIQ password"
   Body: "Click to reset your password"
   Link: /auth/reset-password?token=xxx

Reset password page (/auth/reset-password):
New password input
Confirm password input
[Reset password] button

On submit:
1. Validate token (not expired, not used)
2. Hash new password
3. Update users table
4. Mark token as used
5. Redirect to /login:
   "Password reset successfully. 
    Sign in with your new password."

=== STEP 6: NAVBAR UPDATE ===

Update "Sign in" button behavior:

Currently: links to magic link page

New: links to /login
which has both options

Also add in navbar for logged-out users:
"Sign up" link → /signup

For paid users: keep existing behavior
Show email + sign out

=== STEP 7: PASSWORD TOKENS TABLE ===

CREATE TABLE IF NOT EXISTS 
password_reset_tokens (
  id UUID PRIMARY KEY 
    DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  token TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ DEFAULT
    NOW() + INTERVAL '1 hour',
  used_at TIMESTAMPTZ
);

CREATE TABLE IF NOT EXISTS
email_verification_tokens (
  id UUID PRIMARY KEY
    DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  token TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ DEFAULT
    NOW() + INTERVAL '24 hours',
  used_at TIMESTAMPTZ
);

=== STEP 8: SESSION MANAGEMENT ===

Current: bearer token returned 
from magic link verify

Add: same bearer token system 
for password login

Keep consistent:
Both magic link and password login
return the same JWT/bearer token
Stored in localStorage or httpOnly cookie
Dashboard reads same token either way

Token expiry: 30 days
(so users stay logged in)

=== TEST ALL AUTH FLOWS ===

Test 1 — Sign up with password:
Go to /signup
Enter email + password
Verify: user created in DB
Verify: verification email sent
Verify: redirected to verify-sent page

Test 2 — Sign in with password:
Go to /login
Enter email + password
Verify: session created
Verify: redirected to dashboard

Test 3 — Magic link still works:
Go to /login
Use magic link option
Verify: still works as before

Test 4 — Set password after payment:
Complete test payment
Click magic link
Verify: "Set password" banner shows
Click banner → set password
Verify: can now sign in with password
Verify: banner disappears permanently

Test 5 — Forgot password:
Go to /forgot-password
Enter email
Verify: reset email sends
Click reset link
Set new password
Sign in with new password
Verify: works correctly

Test 6 — Wrong password:
Enter wrong password on /login
Verify: "Incorrect email or password"
Verify: no hint which is wrong
Verify: account not locked after 
  5 wrong attempts
  (just show error, no lockout needed now)

Show screenshots of:
1. /signup page
2. /login page with both options
3. "Set password" banner on dashboard
4. /forgot-password page
5. All 6 tests pass/fail
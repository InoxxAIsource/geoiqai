I need to understand exactly how the 
Brand Benchmarks (Competitor Research) 
feature currently works in the codebase.

Please do a full audit and answer these 
specific questions:

QUESTION 1: DATA SOURCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Where does the competitor score data 
come from right now?

- Is it from DataForSEO API?
- Is it from Claude API?
- Is it hardcoded/hash-based?
- Is it from Supabase stored data?
- Is it from sandbox/mock data?

Show me the exact code that generates 
the competitor scores (the numbers like 
70, 62 etc).

QUESTION 2: AI MENTIONS COUNT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Where does "2.4M AI mentions" and 
"1.6M AI mentions" come from?

- Is this from DataForSEO LLM Mentions API?
- Is this a hardcoded number?
- Is this calculated somehow?

Show me the exact code that produces 
these numbers.

QUESTION 3: TOPICS TABLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Where does the Topics and Prompts table 
data come from?

The table shows:
- Topics like "nfl games", "streamingcommunity"
- AI Volume of 9.1M for every row
- Missing/Weak/Shared/Strong badges

- Is this from DataForSEO?
- Is this real or sandbox data?
- Why is AI Volume 9.1M for every row?

Show me the exact code and API call 
that generates this table.

QUESTION 4: COMPETITOR INSIGHTS TEXT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The right panel shows "COMPETITOR INSIGHTS"
with a paragraph of text analysis.

- Is this generated by Claude?
- What prompt generates it?
- What data is fed into it?

Show me the exact Claude prompt used.

QUESTION 5: TREND CHART
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The AI Visibility Trend chart shows 
two flat lines.

- Where does this data come from?
- Is it real historical data?
- Why are the lines flat?

Show me the code that builds the chart.

QUESTION 6: RESCAN BUTTON
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When user clicks Rescan:
- What API gets called?
- What actually happens?
- Does it hit a real API or return 
  cached/mock data?

Show me the rescan route handler.

QUESTION 7: DATAFORSEO STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Is DATAFORSEO_SANDBOX currently true 
or false in the environment?

Which DataForSEO endpoints are being 
called for this page specifically?

Show me all DataForSEO API calls 
related to competitor research.

QUESTION 8: FULL FILE LIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Which files handle Brand Benchmarks?

List:
- Frontend component file(s)
- Backend route file(s)
- Any utility/helper files

AFTER ANSWERING ALL QUESTIONS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Give me a one paragraph summary:
"Brand Benchmarks currently works by..."

Do not fix anything yet.
Do not change any code.
Just audit and report back.
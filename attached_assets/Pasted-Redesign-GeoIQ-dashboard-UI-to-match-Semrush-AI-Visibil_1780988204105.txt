Redesign GeoIQ dashboard UI to match 
Semrush AI Visibility design quality.
Calm, premium, spacious feeling.
Every sidebar tab opens to a 
feature landing page first.

DESIGN TOKENS — UPDATE GLOBALS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Update CSS variables / Tailwind config:

:root {
  /* Backgrounds */
  --bg-primary: #ffffff;
  --bg-secondary: #f8f9fc;
  --bg-tertiary: #f3f4f8;
  
  /* Borders */
  --border-light: #e8eaef;
  --border-default: #d1d5db;
  
  /* Text */
  --text-primary: #0f172a;
  --text-secondary: #64748b;
  --text-tertiary: #94a3b8;
  
  /* Brand */
  --brand-primary: #6366f1;
  --brand-hover: #4f46e5;
  --brand-light: #eef2ff;
  --brand-text: #4338ca;
  
  /* Status */
  --success: #10b981;
  --warning: #f59e0b;
  --error: #ef4444;
  
  /* Shadows */
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.06);
  --shadow-md: 0 4px 16px rgba(0,0,0,0.08);
  --shadow-lg: 0 8px 32px rgba(0,0,0,0.10);
}

SIDEBAR REDESIGN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.sidebar {
  width: 220px;
  background: var(--bg-primary);
  border-right: 1px solid var(--border-light);
  padding: 16px 0;
  height: 100vh;
  overflow-y: auto;
}

/* Brand logo area */
.sidebar-logo {
  padding: 0 16px 20px;
  border-bottom: 1px solid var(--border-light);
  margin-bottom: 8px;
}

/* Section labels */
.sidebar-section-label {
  font-size: 10px;
  font-weight: 600;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--text-tertiary);
  padding: 12px 16px 4px;
}

/* Nav items */
.sidebar-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  font-size: 13px;
  color: var(--text-secondary);
  cursor: pointer;
  border-radius: 0;
  transition: all 0.15s;
  border-left: 2px solid transparent;
  margin: 1px 0;
}

.sidebar-item:hover {
  background: var(--bg-secondary);
  color: var(--text-primary);
}

.sidebar-item.active {
  background: var(--brand-light);
  color: var(--brand-text);
  border-left-color: var(--brand-primary);
  font-weight: 500;
}

/* Icons */
.sidebar-item .icon {
  width: 16px;
  height: 16px;
  opacity: 0.7;
  flex-shrink: 0;
}

.sidebar-item.active .icon {
  opacity: 1;
}

/* GEO Sprint — special highlight */
.sidebar-item.sprint {
  background: linear-gradient(
    90deg, 
    #eef2ff 0%, 
    #f8f9ff 100%
  );
  color: var(--brand-text);
  font-weight: 500;
}

FEATURE LANDING PAGES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Every tab that has an input (AI Presence,
Brand Benchmarks, Prompt Intelligence,
Signal Tracker, PR Intelligence) should
show a LANDING PAGE before data loads.

Copy Semrush's pattern exactly:
- Gradient orb background
- Category label (small, grey)
- Large headline
- Subheadline
- Input + CTA
- Recently analyzed list

/* Landing page wrapper */
.feature-landing {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60px 40px;
  position: relative;
  overflow: hidden;
  background: var(--bg-primary);
}

/* Gradient orb - Semrush style */
.feature-landing::before {
  content: '';
  position: absolute;
  width: 600px;
  height: 600px;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    rgba(99, 102, 241, 0.08) 0%,
    rgba(139, 92, 246, 0.05) 40%,
    rgba(236, 72, 153, 0.03) 70%,
    transparent 100%
  );
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.feature-landing-inner {
  position: relative;
  z-index: 1;
  text-align: center;
  max-width: 640px;
  width: 100%;
}

/* Category label */
.landing-category {
  font-size: 12px;
  font-weight: 500;
  color: var(--text-tertiary);
  letter-spacing: 0.5px;
  margin-bottom: 16px;
  text-transform: none;
}

/* Main headline */
.landing-headline {
  font-size: 42px;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.15;
  margin-bottom: 16px;
  letter-spacing: -0.5px;
}

/* Subheadline */
.landing-sub {
  font-size: 16px;
  color: var(--text-secondary);
  line-height: 1.6;
  margin-bottom: 40px;
  max-width: 520px;
  margin-left: auto;
  margin-right: auto;
}

/* Input row */
.landing-input-row {
  display: flex;
  gap: 8px;
  max-width: 560px;
  margin: 0 auto 24px;
}

.landing-input {
  flex: 1;
  padding: 14px 20px;
  border: 1.5px solid var(--border-default);
  border-radius: 10px;
  font-size: 15px;
  color: var(--text-primary);
  background: white;
  transition: border-color 0.15s;
  outline: none;
}

.landing-input:focus {
  border-color: var(--brand-primary);
  box-shadow: 0 0 0 3px rgba(99,102,241,0.1);
}

.landing-cta {
  padding: 14px 28px;
  background: var(--brand-primary);
  color: white;
  border: none;
  border-radius: 10px;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  white-space: nowrap;
  transition: background 0.15s, transform 0.1s;
}

.landing-cta:hover {
  background: var(--brand-hover);
  transform: translateY(-1px);
}

/* Recently analyzed */
.landing-recent {
  font-size: 13px;
  color: var(--text-tertiary);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  flex-wrap: wrap;
}

.landing-recent-link {
  color: var(--brand-primary);
  text-decoration: none;
  cursor: pointer;
}

.landing-recent-link:hover {
  text-decoration: underline;
}

LANDING PAGES PER FEATURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Implement landing page for each tab:

AI PRESENCE landing:
Category: "AI Visibility"
Headline: "See Where AI Knows Your Brand"
Sub: "Check how ChatGPT, Gemini, Perplexity, 
      Claude, Grok and Google AI Overview 
      describe your brand — in 60 seconds."
Input placeholder: "Enter domain (e.g. yoursite.com)"
CTA: "Run AI scan"
Recent: "Last checked: [recent domains]"

BRAND BENCHMARKS landing:
Category: "AI Competitor Gap Analysis"
Headline: "Find the Gaps in Your AI Visibility"
Sub: "See how AI platforms position your 
      competitors versus your brand. Uncover 
      topics where rivals get cited — but you don't."
Input 1: "YOU — Enter your domain"
Input 2: "Add competitor"
CTA: "Run competitor analysis"
Recent: [recently analyzed pairs]

PROMPT INTELLIGENCE landing:
Category: "Discover Trending AI Prompts"
Headline: "Uncover the Prompts Where Your Brand Should Be"
Sub: "Find what questions people ask AI, 
      analyze demand and difficulty, and 
      discover opportunities to boost your 
      visibility in AI conversations."
Input: "Enter a topic to analyze"
CTA: "Analyze"
Recent: [recent topics]

SIGNAL TRACKER landing:
Category: "AI Brand Narrative Tracking"  
Headline: "See How AI Talks About Your Brand"
Sub: "Uncover how AI platforms describe, 
      position, and talk about your brand. 
      Get a full breakdown of your AI presence 
      across the most influential prompts."
Input: "Enter your domain"
CTA: "Analyze"
Recent: [analyzed brands]

PR INTELLIGENCE landing:
Category: "AI-Powered PR Toolkit"
Headline: "Get Your Brand Covered by AI-Trusted Media"
Sub: "Find journalists covering AI search, 
      pitch with AI-written emails, and monitor 
      coverage — all powered by real-time 
      web intelligence."
4 action cards (keep existing design)

GEO SPRINT landing:
Category: "30-Day AI Visibility Journey"
Headline: "Go From Invisible to AI-Cited in 30 Days"
Sub: "A research-backed step-by-step plan 
      to get your brand cited by ChatGPT, 
      Gemini and Perplexity. Each step connected 
      to the right GeoIQ tool."
CTA: "Start my sprint →"
No input needed — uses current domain

MAIN DASHBOARD LAYOUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/* Overall app layout */
.app-layout {
  display: flex;
  height: 100vh;
  background: var(--bg-primary);
}

/* Main content area */
.main-content {
  flex: 1;
  overflow-y: auto;
  background: var(--bg-secondary);
}

/* Page header bar */
.page-header {
  background: var(--bg-primary);
  border-bottom: 1px solid var(--border-light);
  padding: 0 32px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: sticky;
  top: 0;
  z-index: 100;
}

/* Page content */
.page-content {
  padding: 32px;
  max-width: 1200px;
  margin: 0 auto;
}

/* Domain search bar (top) */
.domain-bar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background: var(--bg-secondary);
  border: 1.5px solid var(--border-light);
  border-radius: 10px;
  min-width: 320px;
  cursor: pointer;
  transition: border-color 0.15s;
}

.domain-bar:hover {
  border-color: var(--brand-primary);
}

.domain-bar-icon {
  color: var(--text-tertiary);
  font-size: 14px;
}

.domain-bar-text {
  font-size: 14px;
  color: var(--text-primary);
  flex: 1;
}

CARD SYSTEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/* Base card */
.card {
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 14px;
  padding: 24px;
  box-shadow: var(--shadow-sm);
  transition: box-shadow 0.2s;
}

.card:hover {
  box-shadow: var(--shadow-md);
}

/* Metric card */
.metric-card {
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 14px;
  padding: 20px 24px;
}

.metric-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  color: var(--text-tertiary);
  margin-bottom: 8px;
}

.metric-value {
  font-size: 36px;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1;
}

.metric-sub {
  font-size: 12px;
  color: var(--text-secondary);
  margin-top: 4px;
}

/* Section header */
.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
}

.section-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
}

.section-sub {
  font-size: 13px;
  color: var(--text-secondary);
  margin-top: 2px;
}

GAUGE/SCORE REDESIGN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Score gauge cards to match Semrush:
Clean white, no gradients on the card itself.
Gauge sits clean on white.
Color only in the arc and the score number.

.score-gauge-card {
  background: white;
  border: 1px solid var(--border-light);
  border-radius: 14px;
  padding: 28px 24px;
  text-align: center;
}

.score-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  color: var(--text-tertiary);
  margin-bottom: 16px;
}

.score-number {
  font-size: 52px;
  font-weight: 800;
  line-height: 1;
  color: var(--text-primary);
}

.score-status {
  font-size: 13px;
  font-weight: 600;
  margin-top: 4px;
}

/* Status colors */
.status-good    { color: #10b981; }
.status-fair    { color: #f59e0b; }
.status-poor    { color: #ef4444; }

.score-desc {
  font-size: 12px;
  color: var(--text-secondary);
  margin-top: 8px;
  line-height: 1.4;
}

TABLE REDESIGN  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.data-table {
  width: 100%;
  border-collapse: collapse;
}

.data-table th {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--text-tertiary);
  padding: 8px 12px;
  text-align: left;
  border-bottom: 1px solid var(--border-light);
}

.data-table td {
  font-size: 13px;
  color: var(--text-primary);
  padding: 12px;
  border-bottom: 1px solid var(--border-light);
  vertical-align: middle;
}

.data-table tr:hover td {
  background: var(--bg-secondary);
}

.data-table tr:last-child td {
  border-bottom: none;
}

BUTTON SYSTEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/* Primary */
.btn-primary {
  background: var(--brand-primary);
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
}

.btn-primary:hover {
  background: var(--brand-hover);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(99,102,241,0.3);
}

/* Secondary */
.btn-secondary {
  background: white;
  color: var(--text-primary);
  border: 1.5px solid var(--border-default);
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
}

.btn-secondary:hover {
  border-color: var(--brand-primary);
  color: var(--brand-primary);
}

/* Ghost */
.btn-ghost {
  background: none;
  color: var(--brand-primary);
  border: none;
  padding: 8px 12px;
  border-radius: 6px;
  font-size: 13px;
  cursor: pointer;
  transition: background 0.15s;
}

.btn-ghost:hover {
  background: var(--brand-light);
}

BADGE SYSTEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.badge {
  display: inline-flex;
  align-items: center;
  padding: 3px 8px;
  border-radius: 100px;
  font-size: 11px;
  font-weight: 500;
  white-space: nowrap;
}

.badge-error   { bg: #fef2f2; color: #dc2626; }
.badge-warning { bg: #fffbeb; color: #d97706; }
.badge-success { bg: #f0fdf4; color: #15803d; }
.badge-info    { bg: #eff6ff; color: #1d4ed8; }
.badge-purple  { bg: #faf5ff; color: #7e22ce; }
.badge-grey    { bg: #f9fafb; color: #374151; }

/* Soon badge */
.badge-soon {
  background: #f0fdf4;
  color: #15803d;
  font-size: 9px;
  padding: 2px 6px;
}

PLATFORM CHIPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.platform-chip {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 10px;
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 100px;
  font-size: 12px;
  font-weight: 500;
  color: var(--text-primary);
  transition: all 0.15s;
}

.platform-chip:hover {
  border-color: var(--brand-primary);
  background: var(--brand-light);
}

.platform-chip img {
  width: 16px;
  height: 16px;
  border-radius: 3px;
}

.platform-chip .score {
  background: var(--brand-primary);
  color: white;
  font-size: 10px;
  font-weight: 700;
  padding: 1px 6px;
  border-radius: 100px;
}

EMPTY STATES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.empty-state {
  text-align: center;
  padding: 60px 32px;
}

.empty-state-icon {
  font-size: 48px;
  opacity: 0.3;
  margin-bottom: 16px;
}

.empty-state-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 8px;
}

.empty-state-sub {
  font-size: 14px;
  color: var(--text-secondary);
  line-height: 1.6;
  max-width: 360px;
  margin: 0 auto 24px;
}

LOADING STATES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/* Skeleton loader for cards */
.skeleton {
  background: linear-gradient(
    90deg,
    var(--bg-secondary) 25%,
    var(--bg-tertiary) 50%,
    var(--bg-secondary) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 6px;
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

.skeleton-text { height: 14px; width: 60%; }
.skeleton-title { height: 24px; width: 40%; }
.skeleton-block { height: 80px; width: 100%; }

SCROLLBAR STYLING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: var(--border-default);
  border-radius: 100px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--text-tertiary);
}

TRANSITION SYSTEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/* Page transitions */
.page-enter {
  opacity: 0;
  transform: translateY(8px);
}

.page-enter-active {
  opacity: 1;
  transform: translateY(0);
  transition: all 0.2s ease;
}

/* Apply to all main content panels */
Add fade-in animation when any 
sidebar tab is clicked:

const PageWrapper = ({ children }) => (
  <div 
    className="page-wrapper"
    style={{
      animation: 'fadeInUp 0.2s ease forwards'
    }}
  >
    {children}
  </div>
);

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PRIORITY ORDER FOR IMPLEMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Apply in this order:

1. Global CSS variables (design tokens)
   Instant impact everywhere

2. Sidebar redesign
   Most visible first impression

3. Feature landing pages
   AI Presence, Brand Benchmarks, 
   Prompt Intelligence first

4. Card and table styles
   Applied globally

5. Button and badge system
   Applied globally

6. Page transition animation
   Applied to all route changes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Click AI Presence in sidebar
   Should show landing page with
   gradient orb background
   Large headline
   Domain input + CTA
   
2. Click Brand Benchmarks
   Should show Semrush-style landing
   with YOU and competitor inputs

3. Click between tabs quickly
   Smooth fadeInUp animation on each

4. Check sidebar
   Active item has left purple border
   Inactive items are clean grey
   Section labels are subtle uppercase

5. Check cards
   White background, light border
   No heavy shadows
   Generous padding

Share screenshot of AI Presence 
landing page after redesign.
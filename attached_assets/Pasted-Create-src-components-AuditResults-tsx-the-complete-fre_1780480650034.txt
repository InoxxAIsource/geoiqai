Create src/components/AuditResults.tsx — the complete free audit results page shown after audit completes. This must match the homepage design exactly — same #f4f4f0 background, Inter 900 font, same navbar, same dark card style as homepage input box.
STEP 1 — Use these exact AI engine logos (same as thinking screen):
tsxconst AI_ENGINES_CONFIG = [
  { name: "ChatGPT", logo: "https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/openai.svg", color: "#10a37f", bg: "#f0fdf9", hue: "150deg" },
  { name: "Gemini", logo: "https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/googlegemini.svg", color: "#4285f4", bg: "#eff6ff", hue: "220deg" },
  { name: "Perplexity", logo: "https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/perplexity.svg", color: "#20b2aa", bg: "#f0fdfc", hue: "170deg" },
  { name: "Claude", logo: "https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/claude.svg", color: "#cc785c", bg: "#fdf6f3", hue: "20deg" },
  { name: "Grok", logo: "https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/x.svg", color: "#0f172a", bg: "#f8fafc", hue: null },
];
STEP 2 — Component props interface:
tsxinterface AuditResultsProps {
  domain: string;
  score: number;
  enginesFound: number;
  blindSpots: number;
  techScore: number;
  engines: Array<{
    name: string;
    score: number;
    status: string;
  }>;
  techChecks: Array<{
    label: string;
    score: number;
    pass: boolean;
  }>;
  roadmap: Array<{
    week: string;
    target: string;
    label: string;
    detail: string;
    locked: boolean;
  }>;
  isPaid: boolean;
  onReset: () => void;
}
STEP 3 — Full component implementation:
Navbar — identical to homepage:

White background, sticky top, 1px #e5e7eb border bottom
Left: Orbit SVG logo (path d="M32 20 A12 12 0 1 1 20 8" stroke #6366f1 strokeWidth 3, arrow path d="M20 8 L28 8 L28 16", circle cx20 cy20 r2.5 fill #6366f1) + wordmark "Geo" + "IQ" in #6366f1, Inter 800 20px -0.04em
Right: green pulsing badge "Audit complete" — background #f0fdf4, border #bbf7d0, color #16a34a, 6px green dot with pulse animation

Domain row:

Small grey label "AI Visibility Audit · just now" 13px #6b7280
Large domain name Inter 900 22px -0.04em #0f172a below

Score hero card:

Background #0f172a, border-radius 20px, padding 24px
Indigo radial glow top-right: radial-gradient rgba(99,102,241,0.15)
Left side: animated SVG arc ring size 118px

Track circle: stroke #1e293b strokeWidth 7
Progress circle: stroke color based on score (under 35 = #dc2626, under 60 = #d97706, above 60 = #16a34a)
Arc covers 75% of circle starting at 135deg rotation
Score number counts up from 0 to actual score using requestAnimationFrame on mount, duration 1400ms
Score number color matches arc color, font Inter 900 34px
"/100" below in #475569 12px


Right side:

Score label badge: pill shape, background and color based on score level
Headline: "Your brand is invisible to AI right now." — Inter 900 26px -0.04em, color #f1f5f9, "invisible" wrapped in em tag styled italic color #818cf8
Subtext: "0 of 5 AI engines are recommending you." — 13px #475569


Bottom: 3 stat pills in #1e293b background, border-radius 10px:

Engines found — value in #ef4444
Blind spots — value in #f59e0b
Tech score — value in #22c55e



Tab bar:

White background, 1px #e5e7eb border, border-radius 12px, padding 4px
3 tabs: Visibility / Technical / Roadmap
Active tab: #0f172a background, #f1f5f9 text, border-radius 9px
Inactive: #9ca3af text
Inter 700 12px font

VISIBILITY TAB:
White card, title "Brand recognition by AI" Inter 800, subtitle "5 engines checked" grey
For each engine render:
tsx// Engine row
<div style={{ padding: "11px 0", borderBottom: "1px solid #f9fafb" }}>
  <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 7 }}>
    <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
      // Colored dot
      <span style={{ width: 7, height: 7, borderRadius: "50%", background: engineConfig.color, display: "inline-block" }} />
      // Real logo
      <div style={{ width: 22, height: 22, borderRadius: 6, background: engineConfig.bg, display: "flex", alignItems: "center", justifyContent: "center" }}>
        <img src={engineConfig.logo} width={14} height={14}
          style={{ objectFit: "contain", filter: engineConfig.hue ? `invert(40%) sepia(100%) saturate(500%) hue-rotate(${engineConfig.hue})` : "none" }}
          onError={(e) => { (e.target as HTMLImageElement).style.display = "none"; }} />
      </div>
      // Engine name
      <span style={{ fontSize: 13, fontWeight: 700, color: "#111827", letterSpacing: "-0.01em" }}>{engine.name}</span>
    </div>
    // Status pill
    <span style={{
      fontSize: 10, fontWeight: 700, padding: "2px 8px", borderRadius: 100,
      background: engine.score === 0 ? "#fef2f2" : "#fffbeb",
      color: engine.score === 0 ? "#dc2626" : "#d97706",
    }}>{engine.status}</span>
  </div>
  // Animated bar — animate from 0 to engine.score% after 500ms delay
  <div style={{ height: 4, background: "#f3f4f6", borderRadius: 2, overflow: "hidden" }}>
    <div style={{
      height: "100%", borderRadius: 2,
      background: engine.score === 0 ? "#f3f4f6" : engineConfig.color,
      width: barsVisible ? `${Math.max(engine.score, 0)}%` : "0%",
      transition: "width 1.2s cubic-bezier(0.16,1,0.3,1)",
      minWidth: engine.score > 0 ? 5 : 0,
    }} />
  </div>
</div>
Below the engine list — locked box (show only if isPaid is false):

Dashed border 1.5px #e5e7eb, border-radius 10px, background #fafafa, text-align center, padding 16px
Lock emoji large, title "See what Claude, Grok + Google AI say about you" Inter 700 13px
Subtitle "Unlock 4 more AI responses with a paid plan" grey 11px

TECHNICAL TAB:
White card, title "Technical GEO audit", subtitle "6 signals checked"
Each check row:

Icon box 24x24 border-radius 6px: green bg #f0fdf4 with ✅ if pass, amber bg #fffbeb with ⚠️ if warn
Label text 13px #374151 Inter 500
Score "X/100" Inter 800 color green or amber
PASS/WARN pill — green or amber rounded pill

Bottom total row — background #f9fafb border-radius 10px padding 12px:

"Technical GEO Score" label left
Score value right in #16a34a Inter 900 22px

ROADMAP TAB:
White card, title "Your visibility roadmap", subtitle "Score X/100 · path to 80+"
2x2 grid of week cards:

Unlocked card (first): white background, border #c7d2fe, box-shadow 0 0 0 2px rgba(99,102,241,0.06)

Week label grey uppercase 9px
Target score "#6366f1 Inter 900 20px" like "20+ GEO IQ"
Phase label Inter 700 12px #374151
Detail text grey 11px


Locked cards (other 3): background #fafafa, blur filter 2px, lock overlay with 🔒 emoji centered, backdrop-filter blur 2px

CTA card:

Background #0f172a, border-radius 16px, padding 24px, text-align center
Indigo radial glow bottom-right
Headline "Go from invisible to recommended." — Inter 900 24px -0.04em #f1f5f9, "invisible" in italic #818cf8
Subtext: "Get exact tasks, AI-ready content, and a 4-week fix plan. Most brands reach 50+ GEO IQ in 30 days." — 13px #475569
Button: full width, background #6366f1, color white, padding 15px, border-radius 10px, Inter 800 15px, text "Unlock full roadmap — $69/mo", hover darkens to #4f46e5 with translateY(-1px) and box-shadow
Fine print: "7-day free trial · cancel anytime · ₹6,679/mo via Razorpay" — 11px #334155

Two action buttons row:

White background, 1px #e5e7eb border, border-radius 10px, padding 11px, Inter 700 12px #374151
Left button: "↗ Share score on X" — onClick opens:
https://twitter.com/intent/tweet?text=My brand scored ${score}/100 on AI visibility. ${enginesFound}/5 AI engines found me. Checked with @BeingtauheedTk GeoIQ → geoiqai.com

Right button: "↺ Check another domain" — calls onReset prop

Email subscribe card:

White card, border 1px #e5e7eb, border-radius 14px, padding 18px, text-align center
Title "Get free weekly GEO digest" Inter 700 13px
Subtitle "Tips to improve your AI visibility score every week" grey 12px
Row: email input (flex 1, border 1px #e5e7eb, border-radius 8px, padding 10px 12px, Inter 13px) + "Subscribe" button (background #0f172a, white, Inter 700, border-radius 8px, padding 10px 16px)

Page load animations:

All major sections fade up with opacity 0 → 1 and translateY 14px → 0
Stagger delays: 0.05s, 0.12s, 0.19s, 0.26s, 0.33s
Score arc animates on mount with requestAnimationFrame
Engine bars animate after 500ms delay using useState barsVisible

STEP 4 — Wire it up:
Find where audit results are currently rendered in App.tsx or router files. Import and replace with:
tsximport AuditResults from './components/AuditResults';
Pass real audit data from existing state as props. Wire onReset to clear audit state and return to homepage input. Make sure isPaid checks against your existing Supabase user subscription status.
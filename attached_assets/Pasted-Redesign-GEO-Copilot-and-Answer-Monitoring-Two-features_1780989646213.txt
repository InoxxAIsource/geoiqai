Redesign GEO Copilot and Answer Monitoring.
Two features. One prompt.

═══════════════════════════════════════
PART 1: GEO COPILOT REDESIGN
═══════════════════════════════════════

FILE: wherever GEO Copilot component lives

CHANGE 1: HEADER BAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Remove: "Powered by Claude - knows your 
brand, scores, and competitors" subtitle
Remove: "Unlimited messages" text
Remove: "Clear chat" plain text link

Replace with:

<div style={{
  padding: '14px 20px',
  borderBottom: '0.5px solid #e8eaef',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  background: 'white'
}}>
  <div>
    <p style={{
      fontSize: 15,
      fontWeight: 500,
      color: '#0f172a'
    }}>
      GEO Copilot
    </p>
    <p style={{
      fontSize: 12,
      color: '#94a3b8',
      marginTop: 2
    }}>
      {domain} · {score}/100 AI visibility · 
      {remaining} messages left
    </p>
  </div>
  
  <div style={{
    display: 'flex',
    gap: 8,
    alignItems: 'center'
  }}>
    {/* Message usage bar */}
    <div style={{
      width: 80,
      height: 4,
      background: '#f1f5f9',
      borderRadius: 100,
      overflow: 'hidden'
    }}>
      <div style={{
        width: `${(used/limit)*100}%`,
        height: '100%',
        background: used/limit > 0.8 
          ? '#ef4444' : '#6366f1',
        borderRadius: 100,
        transition: 'width 0.3s'
      }} />
    </div>
    <span style={{
      fontSize: 11,
      color: '#94a3b8'
    }}>
      {used}/{limit}
    </span>
    
    <button style={{
      fontSize: 12,
      color: '#64748b',
      background: 'none',
      border: '0.5px solid #e8eaef',
      padding: '4px 10px',
      borderRadius: 6,
      cursor: 'pointer'
    }}>
      Clear
    </button>
  </div>
</div>

CHANGE 2: GREETING MESSAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current broken greeting:
"Hey, I'm your GEO Agent for . Your 
current AI visibility score is 34/100"
(domain missing — shows "for .")

Fix domain insertion AND redesign bubble:

const greeting = `Hi! I'm your GEO Copilot 
for **${domain}**. Your AI visibility score 
is **${score}/100** across 6 AI systems.`;

Show score mini-breakdown inside 
greeting message bubble:

After the text show a 3-column grid:
ChatGPT: {chatgptScore}
Gemini: {geminiScore}  
Perplexity: {perplexityScore}

Pull these from latest AI Presence 
scan cached in Supabase.

CHANGE 3: MESSAGE BUBBLE STYLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AI messages (left side):
- Avatar: 28px circle, #6366f1 bg, 
  white robot icon (ti-robot)
- Bubble: white bg, 0.5px border,
  border-radius: 0 12px 12px 12px
- Padding: 14px 16px
- Font: 13px, line-height 1.6

User messages (right side):
- Avatar: 28px circle, grey bg
- Bubble: #6366f1 background, white text
- border-radius: 12px 0 12px 12px

CHANGE 4: ACTION CHIPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current: plain text bordered chips
"/audit /visibility /authority"
These look like developer commands.

Replace with natural language chips 
shown AFTER each AI message:

const chipStyle = {
  fontSize: 12,
  color: '#6366f1',
  background: '#eef2ff',
  border: 'none',
  padding: '5px 12px',
  borderRadius: 100,
  cursor: 'pointer',
  whiteSpace: 'nowrap'
};

Greeting chips:
["Why is my score low?", 
 "What should I fix first?",
 "Show my sprint progress",
 "Check my competitors"]

After audit response chips:
["Fix the top issue", 
 "Generate the fix",
 "Create content for this",
 "Find journalists"]

After content chips:
["Post to Reddit",
 "Post to LinkedIn", 
 "Write X thread"]

REMOVE the slash commands entirely
from the bottom bar.
Replace with "Quick actions" row:

<div style={{
  display: 'flex',
  gap: 6,
  marginBottom: 10,
  flexWrap: 'wrap'
}}>
  <span style={{
    fontSize: 11,
    color: '#94a3b8',
    alignSelf: 'center',
    marginRight: 4
  }}>
    Quick:
  </span>
  {['Full audit', 'Write content',
    'Find journalists', 
    'Check competitors',
    'My sprint'].map(action => (
    <button key={action} style={{
      fontSize: 11,
      color: '#64748b',
      background: '#f8fafc',
      border: '0.5px solid #e8eaef',
      padding: '3px 10px',
      borderRadius: 100,
      cursor: 'pointer'
    }}>
      {action}
    </button>
  ))}
</div>

CHANGE 5: INPUT AREA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current: basic single-line input

Replace with:

<div style={{
  padding: '12px 16px',
  borderTop: '0.5px solid #e8eaef',
  background: 'white'
}}>
  {/* Quick actions row above input */}
  <QuickActionsRow />
  
  {/* Input row */}
  <div style={{
    display: 'flex',
    gap: 8,
    alignItems: 'center'
  }}>
    <input
      style={{
        flex: 1,
        padding: '10px 14px',
        border: '0.5px solid #d1d5db',
        borderRadius: 8,
        fontSize: 13,
        background: '#f8fafc',
        outline: 'none'
      }}
      placeholder="Ask anything about your AI visibility..."
      onFocus={e => {
        e.target.style.borderColor = '#6366f1';
        e.target.style.background = 'white';
      }}
      onBlur={e => {
        e.target.style.borderColor = '#d1d5db';
        e.target.style.background = '#f8fafc';
      }}
    />
    <button style={{
      padding: '10px 18px',
      background: '#6366f1',
      color: 'white',
      border: 'none',
      borderRadius: 8,
      fontSize: 13,
      fontWeight: 500,
      cursor: 'pointer',
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }}>
      <i className="ti ti-send" />
      Send
    </button>
  </div>
</div>

CHANGE 6: CHAT BACKGROUND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Message area background:
Change from white to very light grey:
background: #f8fafc

This creates visual separation between
the message area and the white bubbles.
Makes bubbles pop visually.

═══════════════════════════════════════
PART 2: ANSWER MONITORING REDESIGN
═══════════════════════════════════════

CHANGE 1: ADD ACCURACY WARNING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

100% Share of Voice with 1/150 prompts
is misleading. Add honest warning.

After the metrics row add:

{trackedCount < 10 && (
  <div style={{
    padding: '10px 20px',
    background: '#fffbeb',
    borderBottom: '0.5px solid #fde68a',
    display: 'flex',
    alignItems: 'center',
    gap: 8
  }}>
    <i className="ti ti-info-circle" 
       style={{ color: '#d97706' }} />
    <p style={{
      fontSize: 12,
      color: '#92400e',
      flex: 1
    }}>
      Share of Voice shows {sov}% because 
      only {trackedCount} prompt is tracked. 
      Add 10+ prompts for accurate data.
    </p>
    <button style={{
      fontSize: 11,
      fontWeight: 500,
      color: '#d97706',
      background: 'none',
      border: 'none',
      cursor: 'pointer'
    }}>
      Add prompts →
    </button>
  </div>
)}

CHANGE 2: METRICS ROW REDESIGN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current: 5 separate bordered cards
Too fragmented. 

Replace with inline grid row:

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(4, 1fr)',
  borderBottom: '0.5px solid #e8eaef'
}}>
  {[
    { 
      label: 'Share of Voice',
      value: `${sov}%`,
      sub: `${trackedCount} of ${trackedCount} · add more for accuracy`,
      warning: trackedCount < 5
    },
    {
      label: 'Mentions',
      value: mentions,
      sub: 'across all AI systems'
    },
    {
      label: 'Tracked',
      value: `${tracked}/${limit}`,
      sub: `${plan} plan`
    },
    {
      label: 'Last checked',
      value: lastUpdated,
      sub: 'auto-checks daily'
    }
  ].map((m, i) => (
    <div style={{
      padding: '16px 20px',
      borderRight: i < 3 
        ? '0.5px solid #e8eaef' : 'none'
    }}>
      <p style={{
        fontSize: 11,
        fontWeight: 500,
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
        color: '#94a3b8',
        marginBottom: 6
      }}>
        {m.label}
      </p>
      <p style={{
        fontSize: 28,
        fontWeight: 500,
        color: m.warning ? '#d97706' : '#0f172a',
        lineHeight: 1
      }}>
        {m.value}
      </p>
      <p style={{
        fontSize: 11,
        color: '#94a3b8',
        marginTop: 4
      }}>
        {m.sub}
      </p>
    </div>
  ))}
</div>

CHANGE 3: TABLE REDESIGN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current columns:
PROMPT | LLM | POSITION | MENTIONED | 
CONTEXT | SENTIMENT | URL CITED | TREND

Problems:
- Context truncated badly
- Trend shows "=" symbol
- Check/dropdown/X cluttered

New column layout:
PROMPT | AI | MENTIONED | POS | 
AI SAID | SENTIMENT | ACTIONS

Changes:
1. "LLM" → "AI" with platform logo+name
2. Context → "AI SAID" 
   Show first 100 chars of actual AI response
   [View full →] link expands inline
3. Remove "URL CITED" column
   Move into expanded row view
4. "TREND" → remove "=" symbol
   Show ↑↗→↘↓ arrow instead
   ↑ = improving, → = stable, ↓ = declining
5. Actions: [View] + [Refresh] only
   Remove dropdown and X (too cluttered)

const getTrendIcon = (trend) => {
  if (trend === 'up') return '↑';
  if (trend === 'down') return '↓';
  return '→';
};

const getTrendColor = (trend) => {
  if (trend === 'up') return '#10b981';
  if (trend === 'down') return '#ef4444';
  return '#94a3b8';
};

Table header styles:
font-size: 11px
font-weight: 500
text-transform: uppercase
letter-spacing: 0.5px
color: #94a3b8
padding: 8px 12px
border-bottom: 0.5px solid #e8eaef

Table row styles:
padding: 14px 12px
font-size: 13px
border-bottom: 0.5px solid #f1f5f9
vertical-align: middle

Row hover: background: #f8fafc

CHANGE 4: EXPAND ROW ON CLICK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When user clicks [View] or clicks row:
Expand inline to show full AI response:

<tr className="expanded-row">
  <td colSpan={7}>
    <div style={{
      background: '#f8fafc',
      border: '0.5px solid #e8eaef',
      borderRadius: 8,
      padding: '14px 16px',
      margin: '4px 0'
    }}>
      <p style={{
        fontSize: 12,
        fontWeight: 500,
        color: '#94a3b8',
        marginBottom: 8,
        textTransform: 'uppercase',
        letterSpacing: '0.5px'
      }}>
        Full AI response
      </p>
      <p style={{
        fontSize: 13,
        color: '#374151',
        lineHeight: 1.7
      }}>
        "{fullResponse}"
      </p>
      <div style={{
        display: 'flex',
        gap: 8,
        marginTop: 12,
        paddingTop: 12,
        borderTop: '0.5px solid #e8eaef'
      }}>
        <span style={{
          fontSize: 12,
          color: '#94a3b8'
        }}>
          URL cited: {urlCited || 'None'}
        </span>
        <span style={{
          fontSize: 12,
          color: '#94a3b8'
        }}>
          · Position: #{position}
        </span>
        <span style={{
          fontSize: 12,
          color: '#94a3b8'
        }}>
          · Checked: {checkedAt}
        </span>
      </div>
    </div>
  </td>
</tr>

CHANGE 5: EMPTY STATE WHEN < 5 PROMPTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After the 1 tracked prompt show 
an encouraging empty state section:

<div style={{
  padding: '40px 20px',
  textAlign: 'center',
  background: '#f8fafc',
  borderTop: '0.5px solid #e8eaef'
}}>
  <i className="ti ti-antenna" 
     style={{ fontSize: 32, color: '#cbd5e1',
              display: 'block', 
              marginBottom: 12 }} />
  <p style={{
    fontSize: 14,
    fontWeight: 500,
    color: '#0f172a',
    marginBottom: 6
  }}>
    Track more prompts for real data
  </p>
  <p style={{
    fontSize: 13,
    color: '#64748b',
    lineHeight: 1.6,
    maxWidth: 300,
    margin: '0 auto 16px'
  }}>
    Add the questions your customers ask 
    AI about your category. You have 
    {limit - tracked} slots remaining.
  </p>
  <button style={{
    padding: '8px 18px',
    background: '#6366f1',
    color: 'white',
    border: 'none',
    borderRadius: 8,
    fontSize: 13,
    fontWeight: 500,
    cursor: 'pointer'
  }}>
    + Add your first 10 prompts
  </button>
</div>

CHANGE 6: SUGGESTED PROMPTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When user clicks [Add prompt] show 
a modal with suggested prompts
based on their domain category:

Use Claude Haiku to generate 10 
relevant prompts for their domain:

POST /api/monitoring/suggest-prompts
Body: { domain }

Claude generates:
"What is the best [category] platform?"
"Which [category] tool do you recommend?"
"[Domain] vs [Competitor] comparison"
etc.

Show as checkboxes in modal:
User checks which to track → [Add selected]

This gets users from 1 to 10+ prompts
in one click.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST BOTH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GEO Copilot:
1. Header shows domain name correctly
   NOT "for ." — shows "for clevertap.com"
2. Message counter shows X/50 or X/200
3. No slash commands visible
4. Action chips show after AI message
5. Chat background is light grey
6. User messages are indigo bubbles
7. Input has focus animation

Answer Monitoring:
1. Yellow warning banner visible
   when < 10 prompts tracked
2. Metrics in 4-column inline grid
3. Context column shows real AI text
   not truncated with "....."
4. Trend shows arrow not "="
5. Actions: View + Refresh only
6. Click row expands full AI response
7. Empty state shows after prompt list
8. [Add prompt] modal suggests 
   10 relevant prompts for domain

Share screenshots of both features.